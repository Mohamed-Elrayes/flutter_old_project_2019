import 'package:flutter/material.dart';

class PersonalDataScreen extends StatelessWidget {

  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('appbarTitle'),
      ),
      body: Container(
        child: Center(
          child: Text('PersonalDataScreen'),
        ),
      ),
    );
  }
}
