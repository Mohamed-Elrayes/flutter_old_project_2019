import 'package:flutter/material.dart';

class ThursdayScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(
      child: Text('Thu'),
    ));
  }
}
