import 'package:flutter/material.dart';

class FridayScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(
      child: Text('جمعه مباركة'),
    ));
  }
}
